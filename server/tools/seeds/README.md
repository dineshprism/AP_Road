Seed and demo assets for the server live here.

Structure:
- `scripts/` - executable TypeScript seed/demo loaders
- `sql/` - SQL helpers, sample data, diagnostics, and setup scripts

Common commands:
- `npm run seed`
- `npm run seed:demo`

These scripts still run from the `server` folder via `package.json`.
